# Correcciones de QA - 2026-08-31

## Alcance y estado

Corregidos localmente los siete hallazgos confirmados de la ultima QA desplegada:
QA-04 a QA-10 (dos High y cinco Medium). No hubo Critical nuevos en esa pasada.
Esto no cierra los ocho pendientes del AUDIT.md anterior ni certifica produccion.
No se hizo commit, push ni despliegue. La QA remota anterior sigue siendo la
referencia del despliegue hasta publicar y revalidar ambos repositorios.

| ID | Severidad | Correccion |
| --- | --- | --- |
| QA-06 | High | Checkout compara cajero y huella canonica de productos/variantes, cantidades, cliente, cupon, caja, turno y pago. Valida las rutas de replay inicial, transaccional y por duplicado. Otra solicitud recibe 409; otro cajero, 404 sin venta. Una venta devuelta no se informa como cobro nuevo. |
| QA-07 | High | UI envia shiftId/cashRegister. API exige ambos o ninguno, verifica turno propio abierto y conserva el cerrojo transaccional con el corte. Movimientos globales siguen disponibles omitiendo ambos campos. |
| QA-08 | Medium | Libro de caja por ID de turno, paginado, con resumen completo calculado por la misma logica del corte dentro de un snapshot Mongo. Incluye fondo inicial, efectivo de mixtos y devoluciones. UI separa historial global y no muestra cero ante errores. Aprobar otro corte ya no reemplaza el turno del supervisor. |
| QA-09 | Medium | Frontend conserva operationType; separa ventas/devoluciones, cantidades brutas/devueltas/netas e ingresos netos. Ticket promedio = importe bruto de ventas / numero de ventas. CSV identifica operacion; top de productos resta devoluciones. |
| QA-04 | Medium | Login expone companyName validado contra la empresa autenticada. Encabezado deja de usar mocks; sesiones antiguas usan un nombre neutral hasta volver a ingresar. No se ampliaron permisos administrativos. |
| QA-05 | Medium | Menu temporal en pantallas pequenas, contenido flexible sin min-width implicito y POS apilado cuando falta espacio. Tabla conserva desplazamiento interno y acceso a carrito/resumen/cobro. |
| QA-10 | Medium | Snapshot de cajero y cliente, fallback de cajero para tickets antiguos, identidad de empresa en cortes y devoluciones, filas monetarias sin heredar columna estrecha, desglose completo cash/card/transfer/mixed y devoluciones en PDF de corte. |

## Validacion

| Grupo | Backend check | Frontend check | Mongo E2E |
| --- | --- | --- | --- |
| QA-06/07 | 62 tests PASS; lint, syntax/typecheck, build PASS | 25 tests PASS; lint, TypeScript, build PASS | 19 PASS |
| QA-08/09 | 62 tests PASS; lint, syntax/typecheck, build PASS | 28 tests PASS; lint, TypeScript, build PASS | 20 PASS |
| Final QA-04/05/10 y regresiones | 64 tests PASS; lint, syntax/typecheck, build PASS | 32 tests / 12 archivos PASS; lint, TypeScript, build PASS | 21 PASS |

Backend `typecheck` es verificacion de sintaxis JavaScript, no tipado TypeScript.
Frontend tambien pasa lockfile y bundle budget. Vite mantiene advertencia por
chunk >900 kB (1188.50 kB decimal, 1160.65 KiB): no se presenta como resuelta.
Git diff --check pasa en ambos repositorios. No se cambiaron dependencias.

Los E2E aprobados usaron MongoDB 8 local temporal, replica set rs0, puerto 27019,
y bases pos_e2e_* creadas y eliminadas por la suite. Prueban HTTP, JWT, propiedad,
concurrencia, rollback, ultimo stock, cupones, devoluciones y cortes. No equivalen
a las 95 comprobaciones remotas anteriores ni a una prueba de carga.

Se detectaron y corrigieron durante regresion un status 500 indebido al vender
sin turno y una espera insuficiente del test durante la animacion de un modal.
No se eliminaron pruebas para obtener resultado verde.

## Evidencia visual y operativa local

- Login con cuenta temporal: encabezado Tienda QA local, rol admin real.
- POS con carrito: viewport/scrollWidth 390/390, 768/768, 952/952 y 1280/1280.
- Menu movil abierto a 390 px y barra compacta a 1280 px no expanden el documento.
- Carrito, resumen y boton COBRAR accesibles en movil; la tabla conserva scroll interno.
- Entrada manual por UI: apertura 100 + entrada 10 = esperado 110; corte contado 110, sin diferencia.
- Venta movil completada por UI: total 116 con recibido 200, carrito vacio y stock verificado en Mongo de 30 a 29. Apertura 100 + venta 116 = corte esperado/contado 216, diferencia cero. Al finalizar, cero turnos locales abiertos.
- Libro HTTP con cuatro pagos, devolucion y movimientos: apertura 500 + entradas 176 - salidas 121 = 555; dos paginas comparten resumen y excluyen movimiento global de 900.
- Reporte de regresion: seis ventas, una devolucion, siete unidades brutas, dos devueltas, cinco netas, ingreso neto 479; promedio bruto 691/6.
- PDFs de 58/80 mm y corte se generaron, renderizaron e inspeccionaron. Nombre de cajero/cliente legible; todos los pagos del corte explican el neto. Evidencia local en ../../qa/fixes/.

## Despliegue y compatibilidad

1. Publicar backend y frontend coordinadamente. Backend primero habilita GET /cash-movements/shift/:shiftId y companyName; el frontend nuevo los consume.
2. Entre despliegues, el frontend antiguo no podra registrar movimientos de caja sin turno. No degradar la validacion para ocultar esta incompatibilidad temporal.
3. Iniciar sesion de nuevo para recibir companyName. No se requieren nuevas variables de produccion ni dependencias.
4. No se reasignaron movimientos historicos sin turno ni se recalcularon cortes cerrados de la demo. Evaluar cualquier reparacion historica por separado, con evidencia y respaldo.
5. Ventas anteriores sin requestFingerprint se comparan contra sus snapshots inmutables. Reintento igual de venta confirmada sigue permitido tras cerrar turno; venta devuelta/cancelada devuelve conflicto.
6. Repetir QA desplegada: venta con reintento, payload distinto, cajero distinto, devolucion, entrada/salida, corte, reportes y PDFs; verificar los tres roles y ambos tenants.

Pendientes fuera de esta tanda: M-03, M-04, M-05, M-06, M-09, M-11, L-01 y L-02
del AUDIT.md original. Tampoco se certifican adquirencia, integracion fiscal,
impresora/escaner, recuperacion de Atlas, TTL prolongado u operacion bajo carga.
